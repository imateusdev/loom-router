# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri
src-tauri/target
src-tauri/gen

# Superpowers execution scratch (never ship with the application)
.superpowers/

# Compozy task artifacts
.compozy/

# Kimi template metadata
template-info.md

# Per-machine cargo settings (target dir, profile tweaks) and the backups
# `cargo` leaves beside a manifest. Both are local to a checkout, and both
# reached main once because a commit was staged with `git add -A`.
**/.cargo/config.toml
*.bak
